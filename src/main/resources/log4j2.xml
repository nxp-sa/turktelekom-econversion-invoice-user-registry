<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" name="RollingKafkaConfig">
    <Properties>
        <Property name="LOG_PATTERN">%d{ISO8601} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_DIR">./logs</Property>
        <Property name="APP_NAME">invoice-user-registry</Property>
    </Properties>

    <Appenders>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <RollingFile name="RollingFile"
                     fileName="${LOG_DIR}/${APP_NAME}/${APP_NAME}.log"
                     filePattern="${LOG_DIR}/${APP_NAME}/archive/${APP_NAME}-%d{yyyy-MM-dd}-%i.log.gz">

            <PatternLayout pattern="${LOG_PATTERN}"/>

            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>

            <DefaultRolloverStrategy max="30"/>

        </RollingFile>

        <Kafka name="KafkaAppender" topic="logs-${APP_NAME}">
            <Property name="bootstrap.servers">192.168.1.101:9092</Property>
            <JsonLayout compact="true" eventEol="true" />
            <Property name="batch.size">16384</Property>
        </Kafka>

    </Appenders>

    <Loggers>

        <Logger name="org.springframework" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Logger name="org.hibernate" level="WARN" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
            <AppenderRef ref="KafkaAppender" level="ERROR"/>
        </Root>
    </Loggers>

</Configuration>